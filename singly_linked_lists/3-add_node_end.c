#include "lists.h"
/**
 * add_node_end - adds new node at the end of list
 * @head: the head pointer
 * @str: string
 *
 * Return: list_t (success) or NULL (failure)
 */
list_t *add_node_end(list_t **head, const char *str)
{
